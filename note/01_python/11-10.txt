웹데이터 수집 >> 전처리 >> 분석 >> DL/ML
#방법1

import requests

url = 'http://~?'
params = {'q':'비트코인','p':1}

headers = {'user-agent':'~'}
response = requests.get(url, params=params, headsers=headers)

#방법 2

from urllib.request import urlopen, Request
request = Request
response = urlopen(request)

# soup 객체
soup = BeautifulSoup(response.text나 response, 'html.parser')


# 엘리먼트 추출하기

select('선택자') / select_one('선택자')

find_all